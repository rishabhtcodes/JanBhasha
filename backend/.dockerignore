# Git & IDE
.git
.gitignore
.gitattributes
.editorconfig
.idea
.vscode

# Local environment secrets — NEVER bake into image
.env
.env.*
!.env.example

# PHP dependencies — installed inside container via composer
vendor

# Node dependencies — installed inside Stage 1 via npm ci
# DO NOT exclude resources/js or resources/css — they are needed by the node_builder stage
node_modules

# Laravel runtime directories — generated at runtime
bootstrap/cache/*
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*
storage/logs/*

# Dev/test files
tests
phpunit.xml
*.phpunit.result.cache

# Docs / misc
README.md
PROJECT_REPORT.md
future-fix.txt
snapshots
brain

# Docker build/deployment config (not needed inside the container itself)
# NOTE: docker-entrypoint.sh is intentionally NOT listed here — it must be
# present in the build context so the Dockerfile can COPY it in.
Dockerfile
.dockerignore
render.yaml
vercel.json
.vercel
